node_modules/
dist/
storybook-static/
*.log
.DS_Store
.worktrees/
worktrees/

# Superpowers brainstorm artifacts
.superpowers/

.claude/scheduled_tasks.lock

# usage-manifest incremental cache (regenerated per machine)
scripts/.usage-manifest-cache.json
.bundle-budget/

# Produced by `npm run test:coverage`; consumed by scripts/execution-coverage.mjs.
coverage/

# Regenerated by `npm run adherence` (post-commit hook + Claude SessionStart
# hook). NOT committed, unlike scripts/health-history.json — see
# docs/adherence/README.md § "Why these two files are not committed".
docs/adherence/OPEN_ITEMS.md
docs/adherence/report.json
