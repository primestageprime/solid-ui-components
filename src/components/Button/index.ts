// Base (Button) is intentionally NOT exported — use curried variants or createButton().
export { createButton } from "./Button";
export type { ButtonDataProps } from "./Button";
export * from "./variants";
