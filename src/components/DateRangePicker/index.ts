// Barrel — only the composed DateRangePicker + its public types are
// exported. Sub-components (CalendarGrid, CalendarHeader, PresetButtons,
// TimeInputs) are private to this folder.
export { DateRangePicker } from "./DateRangePicker";
export type {
  DateRange,
  DateRangePreset,
  DateRangePickerProps,
} from "./types";
