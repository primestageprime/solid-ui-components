// Base (Divider) is intentionally NOT exported — use curried variants or createDivider().
export { createDivider } from "./Divider";
export type { DividerDataProps } from "./Divider";
export * from "./variants";
