// InlineText has no design-config (override) props — colour is data-driven —
// so it is exported directly, no factory/variants (same as Duration/CountChip).
export { InlineText } from "./InlineText";
export type { InlineTextProps } from "./InlineText";
