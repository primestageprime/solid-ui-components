// Base (Tabs) is intentionally NOT exported — use curried variants or createTabs().
export { createTabs } from "./Tabs";
export type { TabsDataProps, Tab, TabStatus } from "./Tabs";
export * from "./variants";
